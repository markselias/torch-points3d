num_workers: 2
batch_size: 1
cuda: 0
weight_name: "Iacc" # Used during resume, select with model to load from [miou, macc, acc..., latest]
enable_cudnn: True
checkpoint_dir: "/tp3d/outputs/2022-03-16/14-25-38" # "{your_path}/outputs/2020-01-28/11-04-13" for example
model_name: PointGroup-PAPER
enable_dropout: False
output_path: "${checkpoint_dir}/out" # Where the output goes
input_path: "/tp3d/data/test" # Folder where to find the data

# Dataset specific
defaults:
    - dataset: ""
      optional: True